#include <stdio.h>
/**
 * main- serves as the entry point
 *
 * Return: zero means succesful and not zero means error
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
